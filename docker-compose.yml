services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: kenyan_dev
      POSTGRES_USER: kenyan
      POSTGRES_PASSWORD: kenyan
    ports:
      - "5433:5432"
    volumes:
      - kenyan_postgres_data:/var/lib/postgresql/data

volumes:
  kenyan_postgres_data:
